import styled from 'styled-components'

export const SortButton = styled.button`
  border: none;
  width: 20px;
  height: 14px;
  padding: 0;
  background: none;

  &:hover {
    cursor: pointer;
  }

  & > img {
    display: block;
    width: 100%;
  }
`
